const Shapes = require('./shapes.js');
class Circle extends Shapes {
    Shapes(Circle) {
      Shapes(Circle); // call the super class constructor and pass in the name parameter
    }
  
    Shapes() {
      console.log(`Circle`);
    }
};

module.exports = Circle