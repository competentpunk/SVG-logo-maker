const Shapes = require('./shapes.js');
class Square extends Shapes {
    Shapes(Square) {
      Shapes(Square); // call the super class constructor and pass in the name parameter
    }
  
    Shapes() {
      console.log(`Square`);
    }
};

module.exports = Square